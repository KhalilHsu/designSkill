# Source Notes

Read this when updating the skill or when checking its boundary against adjacent HTML slide projects.

This skill is informed by public web-slide patterns such as browser-native slide decks, HTML template libraries, and AI-assisted presentation workflows. It should not copy external project wording, preset names, CSS files, JavaScript controllers, or template structures.

The differentiator is AI/database product storytelling:
- Build claims from source material before designing slides.
- Preserve evidence paths for important metrics and charts.
- Use web-native implementation because the product runs in a browser, not because the artifact is trying to clone a presentation tool.
- Treat visual systems as adaptable design directions, not fixed imported templates.

If referencing external inspiration in public docs, say it plainly and keep it high level:
`Inspired by public browser-based presentation and HTML template projects; no external templates or code are bundled.`
